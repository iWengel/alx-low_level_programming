#include "main.h"
#include <stdio.h>
/**
 * add -a function that adds two numbers and outputs the result
 * @a: integer 1
 * @b: integer 2
 * Return: 0 after printing the sum of the numbers
 */
int add(int a, int b)
{
	return (a + b);
}

