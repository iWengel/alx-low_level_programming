#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and returns the result
 * @i: integer 1
 * @j: integer 2
 *
 * Return: the sum of the integers
 */
int add(int i, int j)
{
	return (i + j);
}
