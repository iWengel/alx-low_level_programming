#include "main.h"

/**
 * mul - function that multiplies two integers
 * @a: contains one value of the two integers
 * @b: contains the other value from the two integers 
 * Return: 0 after multiplying the numbers
 */
int mul(int a, int b)
{
	return (a * b);
}
