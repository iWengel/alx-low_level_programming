#include "main.h"

/**
 * mul - function that multiplies two integers
 * @a & @b: contain the values of the two integers
 * Return: 0 after multiplying the numbers
 */

int mul(int a, int b)
{
	return (a * b);
}
